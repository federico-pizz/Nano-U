[pytest]
pythonpath = .
markers =
    gpu: marks tests that require a CUDA GPU (deselect with -m "not gpu")
filterwarnings =
    ignore::DeprecationWarning:tensorflow_model_optimization
    ignore::UserWarning:tensorflow.lite.python.interpreter
