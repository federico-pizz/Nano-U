# Environment Information
# OS:     Linux (Arch)
# Python: 3.12  (REQUIRED — TensorFlow ships no wheels for 3.13/3.14)
# CUDA:   13.1
# GPU:    NVIDIA GeForce RTX 5060 Laptop GPU
#
# Versions below are pinned to the known-good, reproducible CPU/standard-GPU set.

tensorflow==2.20.0
tf_keras==2.20.1
tensorflow-model-optimization==0.8.0
numpy==1.26.4
pandas==2.3.3
matplotlib==3.10.8
opencv-python==4.11.0.86
PyYAML==6.0.3

# Tests
pytest==9.0.2

# --- NVIDIA GPU (optional, not pinned) ---
# The pins above are CPU-only so they install everywhere. For a GPU:
#     pip install "tensorflow[and-cuda]==2.20.0"
# Very new cards (RTX 50-series / Blackwell, sm_120) need CUDA 12.8+ and a TF
# nightly — see "GPU (optional)" in README.md.
