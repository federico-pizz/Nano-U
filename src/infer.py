"""Inference script stub for Nano-U models.

Adapt this to load a model from `models/` and perform prediction on
images in `data/processed/` or `data/raw/`.
"""

def infer(image_path: str):
    print(f"Would run inference on {image_path}")


if __name__ == "__main__":
    infer("data/processed/test/img/sample.png")
