"""Utility helpers for training/inference (data loaders, metrics wrappers)."""


def load_model(path: str):
    print(f"Loading model from {path}")
    return None


def save_results(results, out_dir: str):
    print(f"Saving results to {out_dir}")
